    <h2>Report for: <%= report_datetime_formatted(@report.date) %></h2>

    <h2>Add Birds Banded:</h2>
    <table>
    <tr id="header_row"><th>Alpha Code</th><th>Species Name</th><th>Number Banded</th></tr>
    <%= form_for(:birds_of_species) do |f| %>
        <%= f.hidden_field :report_id, :value => @report.id %>
        <%= f.hidden_field :bander_id, :value => current_bander.id %>
        <%= f.fields_for :species do |s|%>
            <tr>
                <td class="<%= 'field_with_errors' if code_error_exists(@report) && last_record?(f, report) %>"><%= s.text_field :code %></td>
                <td class="<%= 'field_with_errors' if name_error_exists(@report) && last_record?(f, report) %>"><%= s.text_field :name %></td>
                <td><%= f.number_field :number_banded %></td>
                
            </tr>
        <%end%>
    </table>

    <%= f.submit "Add More" %>
    <%end%>

